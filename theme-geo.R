theme_geo <- theme(
  panel.grid.major = element_line(color = "grey93", size = 0.3),
  panel.grid.minor = element_blank(),
  axis.title = element_blank(),
  axis.text = element_text(family = "IBM Plex Mono"),
  plot.title = element_text(family = "IBM Plex Sans", size = 19, margin = margin(0, 0, 10, 0)),
  plot.subtitle = element_text(family = "IBM Plex Sans", margin = margin(0, 0, 10, 0)),
  plot.caption = element_text(family = "IBM Plex Sans", margin = margin(10, 0, 0, 0)),
  plot.margin = margin(20, 20, 20, 20)
  ) 